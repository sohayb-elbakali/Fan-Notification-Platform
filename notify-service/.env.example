# Notify Service Environment Variables
# In production, these are injected from GCP Secret Manager via --set-secrets

# ==================== LOCAL DEVELOPMENT ====================
# Use 9001 locally to avoid conflict with backend API (8080)
PORT=9001

# Shared token for authentication (must match Lambda's GCP_NOTIFY_TOKEN)
# In production: comes from secret notify-token
NOTIFY_TOKEN=dev-secret-token

# Logging level
LOG_LEVEL=debug

# ==================== PRODUCTION (GCP) ====================
# Port is 8080 in Cloud Run (set automatically)
# Secrets are mounted automatically via:
# gcloud run deploy --set-secrets "NOTIFY_TOKEN=notify-token:latest"
