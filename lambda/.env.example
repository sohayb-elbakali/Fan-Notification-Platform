# Lambda Environment Variables
# These are configured in AWS Lambda console or via CLI

# GCP notify-service URL
GCP_NOTIFY_URL=https://can2025-notify-service-xxxxx.run.app/notify

# Shared secret token (must match NOTIFY_TOKEN in notify-service)
GCP_NOTIFY_TOKEN=your-shared-secret-token
