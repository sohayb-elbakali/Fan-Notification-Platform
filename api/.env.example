# Azure SQL Database
DB_SERVER=your-server.database.windows.net
DB_NAME=can2025
DB_USER=admin
DB_PASSWORD=your-password

# AWS Lambda Webhook
AWS_LAMBDA_URL=https://xxx.lambda-url.region.on.aws
WEBHOOK_TOKEN=shared-secret-token

# Server
PORT=3000
NODE_ENV=development
