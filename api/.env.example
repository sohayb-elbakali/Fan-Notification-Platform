# Azure SQL Database
DB_SERVER=your-server.database.windows.net
DB_NAME=can2025
DB_USER=admin
DB_PASSWORD=your-password

# AWS Lambda Function URL (for calling event processor)
# In production: https://xxxxx.lambda-url.eu-west-1.on.aws/
# For local testing without Lambda, leave empty (events will be logged only)
LAMBDA_FUNCTION_URL=

# Server
PORT=8080
NODE_ENV=development

# Webhook Token (for local testing)
WEBHOOK_TOKEN=dev-secret-token
