# Backend API Environment Variables
# In production, these are injected from GCP Secret Manager via --set-secrets

# ==================== LOCAL DEVELOPMENT ====================
NODE_ENV=development
PORT=8080

# Azure SQL Database (leave empty for mock mode)
# In production: comes from secrets azure-db-server, azure-db-name, azure-db-user, azure-db-password
DB_SERVER=
DB_NAME=
DB_USER=
DB_PASSWORD=

# AWS Lambda Function URL
# In production: comes from secret lambda-function-url
# Leave empty locally - events will just be logged
LAMBDA_FUNCTION_URL=

# Webhook Token (for local testing)
WEBHOOK_TOKEN=dev-secret-token

# ==================== PRODUCTION (GCP) ====================
# Secrets are mounted automatically via:
# gcloud run deploy --set-secrets "DB_SERVER=azure-db-server:latest,..."
